#pp-newprojectlink {
    a:hover {
        text-decoration: none;
    }
}

.pp-newProjectDialog {
    input, textarea {
        padding: 0 12px 0 12px;
    }

    .advanced-settings {
        .toggle-section {
            cursor: pointer;
            position: relative;
            padding: 10px 0 5px 0;
            span.ChevronUp, span.ChevronDown {
                position: absolute;
                right: 5px;
                top: 14px;
            }
        }
    }
}
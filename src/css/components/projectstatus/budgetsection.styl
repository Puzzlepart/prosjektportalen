.budget-section {
    .economy-status {
        padding: 20px;
        .status-elements {
            display: flex;
            justify-content: space-between;

            .prop {
                flex: 0 1 auto;
                text-align: center;

                ._label {
                    font-size: medium;
                }

                ._value {
                    font-size: xx-large;
                }
            }
        }
    }
}
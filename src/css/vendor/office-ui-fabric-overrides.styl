.ms-Panel-main {
    .ms-Panel-content {
        /**  https://github.com/Puzzlepart/prosjektportalen/issues/90 **/
        max-height: 95%;
        padding-bottom: 20px;
    }
}

// Actually an overwrite of core15.css - messes up office ui fabric toggle buttons
input[type=button], input[type=reset], input[type=submit], button {
    min-width: 2.2em; //Default 6em
}

input.ms-TextField-field, input.ms-SearchBox-field {
    border: none;
    outline: none !important;
}
